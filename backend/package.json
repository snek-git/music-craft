{
  "name": "music-craft-backend",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "db:seed": "bun run src/db/seed.ts",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "drizzle-orm": "^0.36.0",
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0"
  }
}
